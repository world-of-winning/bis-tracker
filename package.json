{
  "name": "wow-bis-tracker",
  "private": true,
  "version": "2.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "bun run build && wrangler dev --port 15358 --inspector-port 15359",
    "release": "changelogen --release",
    "prepare": "husky",
    "deploy": "bun run build && wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@cloudflare/vite-plugin": "^1.30.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "wrangler": "^4.77.0"
  },
  "changelogen": {
    "repo": {
      "provider": "github",
      "repo": "world-of-winning/bis-tracker"
    }
  },
  "devDependencies": {
    "@commitlint/cli": "^20.5.0",
    "@commitlint/config-conventional": "^20.5.0",
    "@vitejs/plugin-react": "^4.3.4",
    "changelogen": "^0.6.2",
    "cheerio": "^1.2.0",
    "husky": "^9.1.7",
    "vite": "^6.0.0",
    "vitest": "4.1.11"
  }
}
